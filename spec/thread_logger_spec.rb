require 'spec_helper'

describe ThreadLogger do
  it 'has a version number' do
    expect(ThreadLogger::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
